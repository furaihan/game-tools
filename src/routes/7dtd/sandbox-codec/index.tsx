import { createFileRoute } from '@tanstack/react-router'
import { SandboxCodec } from '@/components/sandbox-codec/SandboxCodec'

export const Route = createFileRoute('/7dtd/sandbox-codec/')({
  component: SandboxCodec,
})
